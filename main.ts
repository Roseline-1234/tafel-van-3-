let getal = 0
basic.forever(function () {
    if (getal < 12) {
        basic.showNumber(0)
        basic.showNumber(3)
        basic.showNumber(6)
        basic.showNumber(9)
        basic.showNumber(12)
        basic.pause(1000)
    } else {
        getal += 0
    }
})
